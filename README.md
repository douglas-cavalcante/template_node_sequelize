# template_node_sequelize
Esse projeto disponibiliza uma estrutura padrão para iniciar um projeto de api node com sequelize.

Intruções: 
 - clone o repositório
 - execute <b>yarn install ou npm install</b> na raiz do projeto.
 - Aproveite !!!
